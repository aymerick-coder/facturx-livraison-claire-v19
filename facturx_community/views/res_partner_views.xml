<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- FIX 26/06 (retour Claire HANZO / Logitud) : on N'AJOUTE PLUS de champ
         SIRET custom sur la fiche partenaire. Le SIRET se saisit dans le champ
         NATIF Odoo `company_registry` (affiché par la localisation française
         l10n_fr sur les contacts FR). Évite le doublon de donnée signalé par
         Claire. Le champ `siret` reste dans le modèle (synchronisé avec
         company_registry) pour la rétro-compatibilité du code, mais n'est plus
         exposé en UI. -->

    <!-- Code service + EJ Chorus Pro : AU NIVEAU FACTURE uniquement (retour
         Claire 08/06), donc rien ici non plus. -->

</odoo>
